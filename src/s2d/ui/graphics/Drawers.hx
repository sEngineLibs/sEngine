package s2d.ui.graphics;

class Drawers {
	public static var rectDrawer:RectDrawer = new RectDrawer();

	static function ready() {
		rectDrawer.ready();
	}

	static function set() {
		rectDrawer.set();
	}
}
